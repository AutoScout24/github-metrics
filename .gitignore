# These patterns are project specific, for your development environment-specific
# ignores such as .idea or .DS_Store use a local ~/.config/git/ignore file

dist
cdk.out/
cdk.context.json
node_modules
.DS_Store
yarn-error.log