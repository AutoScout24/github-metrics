{
  "name": "github-metrics-deploy",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "format:fix": "prettier --ignore-path ../.gitignore --write .",
    "format:test": "prettier --ignore-path ../.gitignore --list-different . || (echo 'Problem with code format. Please do yarn format:fix' && exit 1)"
  },
  "devDependencies": {
    "@types/node": "^15.0.1",
    "aws-cdk": "^2",
    "ts-node": "^9.1.1",
    "typescript": "4.2.4"
  },
  "dependencies": {
    "@autoscout24/aws-cdk": "^v2",
    "constructs": "^10.1",
    "aws-cdk-lib": "^2"
  }
}
